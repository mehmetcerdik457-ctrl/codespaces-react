name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  baseline:
    uses: mehmetcerdik457-ctrl/.github/.github/workflows/reusable-node-baseline.yml@fea0cdba87dd09c88648eb29302e65c32ba7fd44
    with:
      node-version: '22'
      run-tests: true
      run-build: true
